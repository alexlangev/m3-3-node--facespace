<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Facespace</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="header">
      <a class='site-title' href="http://localhost:8000/">Facespace</a>
      <% if (currentUser.name) { %>
        <h3 class="greeting">Howdy, <%= currentUser.name %>!</h3>
      <% } else { %>
        <a class="sign-in-link" href="http://localhost:8000/signin">Sign In</a>
      <% } %>
  </header>